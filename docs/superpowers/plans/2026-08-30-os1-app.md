# OS1 desktop app — rebrand, limitations, packaging

**Branch:** `feat/os1-app` (off `main` @ 9eecada54, which now has execution-nodes merged)
**Goal:** Ship an openable, OS1-branded macOS desktop app for the execution-node
system, with the create/edit agent limitations fixed.

## Live environment (do not re-derive)
- Node: `ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR -J jump@52.117.123.191 shiv@10.218.128.28` (repo `~/os1`). Always `-o ServerAliveInterval=30`.
- Relay (self-hosted on node): `ws://100.88.16.15:3000` (Tailscale IP). Community "WorkOS1" (fa7ce6ce), owner = user `dbdb7267…` ("shivers").
- Node pubkey `f633923b…`; OpenAI key in the node's provider secret store; `agent_env` = codex defaults.
- Desktop launch (dev): `just desktop-node` (identity from `~/.buzz-owner.nsec` = user's key).
- OS1 assets: `/Users/shiv/workspace/codex/2026-08-30/browser-plugin-browser-openai-bundled-x20/outputs/os1-assets/` (AppIcon.icns/.iconset, OS1StatusTemplate, waveform/ring/wallpaper).
- OpenAI key (never print): `security find-generic-password -a shiv -s OPENAI_API_KEY -w`.

## Workstream A — fix limitations
- [ ] **A1 Node display name** — add a human name to `NodeCapabilities` (kind 39500 announce) so the desktop "Run on" dropdown shows a name, not the pubkey. buzz-node `daemon.rs` up_foreground + `buzz-core` NodeCapabilities + desktop `nodesStore`/`WhereToRunSection`.
- [ ] **A2 Model propagation** — convey the agent's model to the node the same way as the system prompt (assignment `launch.env`, e.g. a buzz-acp model env). Only matters for non-default models.
- [ ] **A3 Live-edit propagation** — editing a running node agent's instructions must reach the node: desktop edit → re-publish the 39502 assignment (new `BUZZ_ACP_SYSTEM_PROMPT`), and the node engine must **restart** the agent on a launch/secret change (today it adopts a running process). buzz-node `engine.rs` + desktop `useAgentManagement` update path. (Biggest.)

## Workstream C — rebrand to OS1 (do before packaging)
- [ ] **C1 Assets** — copy OS1 icons into `desktop/src-tauri/icons/` (icon.icns + iconset PNGs) + menu-bar template; add brand graphics to `desktop/src` where the UI shows the Buzz logo.
- [ ] **C2 Tauri config** — `productName` → "OS1", window title, bundle identifier, icon references (`desktop/src-tauri/tauri.conf.json` + the dev config in `scripts/instance-env.sh`).
- [ ] **C3 UI text/logo** — replace user-facing "Buzz" with "OS1" (welcome/onboarding, sidebar, about, titles) + swap logo art. Scope via a grep for user-facing "Buzz" strings; do NOT rename code identifiers/crates/package names (keep `buzz-*` internal).
- [ ] **C4 Verify** — dev app shows OS1 branding end to end.

## Workstream B — package a macOS .app
- [ ] **B1 Build config** — bake the relay URL for the packaged app (`BUZZ_DESKTOP_BUILD_RELAY_URL=ws://100.88.16.15:3000`) OR rely on onboarding "join community"; ad-hoc/local signing (no notarization/publishing).
- [ ] **B2 Build** — `tauri build` (via a `just` recipe) → `OS1.app` under `desktop/src-tauri/target/release/bundle/macos/`.
- [ ] **B3 Identity** — first-run: import the user's nsec via the UI (app supports import) so the .app is *them*; confirm it connects to WorkOS1 + the node shows in "Run on".
- [ ] **B4 Recipe + docs** — a `just os1-app` recipe that builds + optionally installs to /Applications; note how to open it.

## Approach
- Rebrand (C) first, then limitations (A), then package (B) so the .app is OS1-branded with fixes.
- Use focused subagents for isolated chunks (rebrand grep+edit, each limitation) to preserve context; do builds/live-tests myself.
- Commit per completed task; push to `feat/os1-app`. Run fmt/clippy/tsc before pushing (pre-push gate).
- `/loop` drives continuation; update this file's checkboxes as tasks complete.

## Status snapshot (latest)
- **DONE + committed:** merge→main (9eecada54); C rebrand (8eac2ece1); B packaging `just os1-app` (ca9deaabe, /Applications/OS1.app installed); A1 node name (3df3831d0); A2 model (cc5d54651). feat/os1-app pushed through ca9deaabe (A1/A2 not yet pushed).
- **IN PROGRESS:** A3 live-edit — node change-detection (subagent) + desktop edit re-publish (subagent), parallel.
- **A3 validation (autonomous limits):** owner_driver can't re-assign the desktop's agents (node owner is the user's key dbdb7267, owner_driver is 6220f33), and the edit action needs the GUI — so I can't fully live-test edit→restart autonomously. Autonomous safety net = A3-node UNIT tests (stale→Restart, match→Noop, unknown→Noop) + all pre-existing buzz-node tests passing (adoption/survival invariants) + a LIVE adoption check (deploy A3-node → restart daemon → confirm the running agent is ADOPTED, not restarted, and still responds). The end-to-end edit→restart flow is for the USER to confirm in the app.

## Progress log
- 2026-08-30: merged execution-nodes → main (9eecada54); cut feat/os1-app; wrote this plan.
- 2026-08-30: reprioritized to C (rebrand) → B (package) → A (limitations) since the openable OS1 app is the headline ask. `/goal` + `/loop` are UI-only commands I can't invoke; running continuously with this file as the tracker instead.
- 2026-08-30: dispatched rebrand agent (desktop icons/config/UI, guarded to user-facing only). Started release sidecar build in tmux `sidecars` in parallel. Packaging plan locked: host `aarch64-apple-darwin`; build real sidecars → `BUZZ_DESKTOP_BUILD_RELAY_URL=ws://100.88.16.15:3000 pnpm tauri build` (no mesh-llm) → ad-hoc sign → `just os1-app` recipe. Awaiting both builds, then commit rebrand + package.
- 2026-08-30: **C (rebrand) DONE** — committed 8eac2ece1 (icon, tray→OS1StatusTemplate, productName/title/Info.plist "OS1", os1-mark logo, UI text; guardrails respected). Fixed a TS18046 in the (unreachable) clean-slate catch that blocked tsc/build.
- 2026-08-30: **B (packaging) DONE** — `just os1-app` builds a lean (no mesh-llm) OS1.app with the WorkOS1 relay baked (`ws://100.88.16.15:3000`), ad-hoc signed, and installs to `/Applications/OS1.app`. Verified it launches + initializes app-data. DMG step skipped (`--bundles app`; bundle_dmg.sh is flaky locally). Recipe committed ca9deaabe. Pushed feat/os1-app checkpoint.
- 2026-08-30: starting **A (limitations)**: A1 node name → A2 model → A3 live-edit (riskiest, last). A1 note: desktop already renders `node.name` (WhereToRunSection:60); it falls back to the pubkey because `NodeCapabilities` (buzz-core/node.rs) has no name field.
- 2026-08-30: **A1+A2 delegated** to a subagent (buzz-core NodeCapabilities.name + buzz-node NodeConfig.name/hostname + nodesStore.ts:402 `caps.name ?? truncatePubkey`; A2 = convey model via launch.env like the system prompt, only if buzz-acp has a model env). Awaiting it (can't edit buzz-node concurrently).
- 2026-08-30: **A3 design** (implement after A1+A2). Node: `reconcile.rs` emits `Action::Restart` for a desired+Assigned agent that is `Running` when a NEW `applied: &BTreeMap<PublicKey,u64>` param says its applied secret-hash != the desired secret's hash; `Noop` when applied is unknown (adopted → never spuriously restarted) or equal. `engine.rs` maintains `applied` (insert hash on apply of Start/Restart, remove on Stop) + passes it to `reconcile`; add a `desired_hash(&AssignmentSecret)` over launch.command/args/env/policy_env/env_vars. Update all reconcile call sites + the ~15 reconcile.rs tests. Desktop: the edit/update path re-publishes the 39502 for node-hosted agents (new BUZZ_ACP_SYSTEM_PROMPT). Note `Observed` is a status-only enum — do NOT put the hash there; the separate `applied` map keeps reconcile pure + the substrate unchanged. TEST via owner_driver on the node: assign → running → re-assign with changed launch.env → observe Restart + new prompt; and confirm a daemon restart still ADOPTS (no spurious restart). Risky (engine invariants) → live-test before trusting.
